import { LogoWrapper, LogoMain, LogoText} from "./Logo.styled"

const Logo = () => {
    return(
        <LogoWrapper>
            <LogoMain>
                M
            </LogoMain>
            <LogoText>
                entorsboard
            </LogoText>
        </LogoWrapper>
    );
};

export default Logo;